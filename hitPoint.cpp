#include "hitPoint.h"

hitPoint::hitPoint(Vector _pos, Vector _dir, Vector _col, int _indexX, int _indexY, double _prob, double _rad):
	pos(_pos), dir(_dir), col(_col), indexX(_indexX), indexY(_indexY), prob(_prob), rad(_rad) {}
